/*********
 * entry *
 *********/
.entry {
    padding: 4px 7px;
    border-style: none;
    border-radius: 0px;
    background-color: shade(@theme_bg_color, 0.92);
    background-image: none;
    color: @theme_text_color;
    box-shadow: 1px 1px 2px alpha(@theme_fg_color, 0.2) inset,
		-1px -1px 2px alpha(@theme_fg_color, 0.2) inset;
}

.entry:active,
.entry:focus {
    transition: 200ms ease-in;
    background-color: shade(@theme_bg_color, 0.97);
    background-image: none;
    box-shadow: 1px 1px 2px alpha(@theme_fg_color, 0.15) inset,
		-1px -1px 2px alpha(@theme_fg_color, 0.15) inset;
}

.entry:selected,
.entry:selected:focus {
    background-color: @theme_selected_bg_color;
    color: @theme_selected_fg_color;
}

.entry:insensitive {
    background-color: shade(@theme_bg_color, 0.98);
    background-image: none;
    color: shade(@theme_bg_color, 0.8);
    box-shadow: 1px 1px 2px alpha(@theme_fg_color, 0.1) inset,
		-1px -1px 2px alpha(@theme_fg_color, 0.1) inset;
}

.entry.progressbar {
    border-width: 0px;
    border-radius: 0px;
    background-color: @theme_selected_bg_color;
    background-image: none;
    color: @theme_selected_fg_color;
}

